# Project: ray-no-fork

This repository is managed by an external orchestrator. Agents should follow instructions in `.opencode/`.

## Code Conventions
- Follow existing code style in the repository
- Do not introduce new dependencies without explicit approval
- No force pushes
